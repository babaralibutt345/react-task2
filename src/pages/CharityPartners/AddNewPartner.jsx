import React from "react";

const AddNewPartner = () => {
    return (
        <div>
            <h2>Add New Charity Partner</h2>
        </div>
    );
};

export default AddNewPartner; // ✅ Default export required
