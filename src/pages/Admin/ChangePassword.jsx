import React from "react";

const ChangePassword = () => {
    return (
        <div>
            <h2>Change Password Page</h2>
        </div>
    );
};

export default ChangePassword;  // ✅ Default export
