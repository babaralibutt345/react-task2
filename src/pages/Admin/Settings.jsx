import React from "react";

const AdminSettings = () => {
    return (
        <div>
            <h2>Admin Settings Page</h2>
        </div>
    );
};

export default AdminSettings; // ✅ Default export
